# Product Service

This service handles product management for the E-commerce backend.

## Features
- Product listing
- Product details
- Inventory management

## Running locally

```bash
npm run start:dev
```

## Database
Uses Postgres. Ensure `PRODUCT_DB_NAME` is set in `.env` or defaults to `product_db`.
